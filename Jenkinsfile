/* Requires the Docker Pipeline plugin */
pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'Build'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploy'
            }
        }
    }
}